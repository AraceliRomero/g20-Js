// !Metodos de strings  (los trata como objetos)

// Los 'fer'  'f', 'e'. 'r'
              0,   1,  2

let testObject = {
    first_name: '',
    las_name: '',
    modulo: ''


}

// length -> Propiedad 
    //nos da la longitud del stringTes

//Metodos: (Ninguno de los metodos de string modifica el string original, en caso de requerirlo tenemos que hacerlo permanecer en alguna variable)
//  chartAt(<index al cual acceder>)
    //Devuelve el caracter del index 

// toUpperCase( )
    // Nos retorna un string en mayuscula 

// toLowerCase ( )
    // Nos retorna un string en minusculas

// subString (<posicion inicial> , ?<posicion final>)
    // Retorna la porcion de string indicada

// slice (<posicion inicial> , ?<posicion final>)
    // Retorna la porcion de string indicada
    // Acepta numero negtivos (los lee del final hasta el inicio)

// indexof (<vlor a buscar>, <?Poscion inicial>)
    // Regresa el indice de la primera coincidencia del caracter indicado
    // En caso que no lo encuentre regresa -1

//replace ('o', 'X')(<valo a buscar>, <valor de cambio>)
    //Cambia la primera coincidencia del valor indicado

//replace ('o', 'X')(<valo a buscar>, <valor de cambio>)
    //Cambia la primera coincidencia del valor indicado
    
//replaceAll ('o', 'X')(<valo a buscar>, <valor de cambio>)
    //Cambia todas las coincidencias del valor indicado

// Trim ( )
    // Limpia el string de espacio al final y el inicio

// let koder = 'hola koder'
// let vocales = koder.match(/[aeiou]/gi)
// console.log (vocales)



//     let stringValidation = steingTest.indexOf('z')

//     if (stringValidation == -1_) {
//             // code
//     }
//     else {
//         // code
//     }

// <p> prueba</p>
